
  # Generate RedTape Mobile UI

  This is a code bundle for Generate RedTape Mobile UI. The original project is available at https://www.figma.com/design/LjQenoRRYcoj7cXlJWazHf/Generate-RedTape-Mobile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  